{
  "title": "Jupyternaut Terminal",
  "description": "Settings of the jupyternaut terminal command.",
  "type": "object",
  "properties": {
    "fullScreen": {
      "type": "boolean",
      "title": "Full screen mode",
      "description": "Run the agent in the alternate screen buffer with its own scrolling, so the prompt stays at the bottom while the transcript scrolls. When disabled, the transcript is printed in the terminal scrollback and the prompt scrolls with it.",
      "default": true
    },
    "engine": {
      "type": "string",
      "title": "Agent engine",
      "description": "The agent runtime of the jupyternaut command: 'ai-sdk' is the Jupyternaut agent built on the Vercel AI SDK, 'pi' is the pi-agent-core loop driven with the same model providers and tools. Use `jupyternaut --engine <name>` for one run.",
      "enum": ["ai-sdk", "pi"],
      "default": "ai-sdk"
    },
    "ui": {
      "type": "string",
      "title": "Terminal UI",
      "description": "The user interface of the jupyternaut command: 'builtin' is the renderer of this extension (inline or full screen), 'pi' is the pi TUI (pi-tui) on the main screen. Use `jupyternaut --ui <name>` for one run.",
      "enum": ["builtin", "pi"],
      "default": "builtin"
    }
  },
  "additionalProperties": false
}
